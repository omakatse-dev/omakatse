import React from 'react'

// this component will receive boxid as a prop, fetch the box details and display the info
export default function ViewBoxDetails() {
  return (
    <div>
        {/* Follow Subscription Card Component */}
    </div>
  )
}
